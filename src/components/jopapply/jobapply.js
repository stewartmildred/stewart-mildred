import React from "react";
import Form from "../Form/form";

function Header (){
   return(<>
    <div>
   <h3>
   We are Currently hiring for the role of a remote secretary.
   </h3>
   <p>View job description</p>
   <p>Fill the form to apply below</p>
   <Form/>
    </div>
   </>
   )
}

export default Header